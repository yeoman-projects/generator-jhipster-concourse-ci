---
platform: linux

image_resource:
    type: docker-image
    source: {repository: anoop2811/java8-gradle} #Switch to a public repo
inputs:
- name: test

outputs:
- name: ccs-auth-provisioning-build
params:
    okta.user.username: ((OKTA_USER.username))
    okta.user.password: ((OKTA_USER.password))
    GRADLE_ARGS:
run:
    path: ci/task/gradle-build-task.sh
caches:
- path: ~/.gradle/
